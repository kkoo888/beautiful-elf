# 对话模块三模式模拟推理分析（1000次/模式）

## 模拟假设
- 不考虑上下文长度限制
- 不考虑 token 成本
- 不考虑多用户并发
- LLM 响应延迟恒定 500ms
- 工具调用延迟恒定 200ms

---

## 模式一：普通模式（Normal Mode）

### 流程链路
```
用户消息 → model_selector(ML路由) → intent_router(向量匹配)
  ├─ 闲聊命中 → 直接回复 (<1ms)
  ├─ 语义缓存命中 → 返回缓存答案 (<5ms)
  ├─ 意图命中 → skill_executor → 回复
  └─ 未命中 → context_builder → llm_call → [tool_executor → llm_call]* → evaluator → memory_saver
```

### 1000次模拟结果分析

| 场景 | 预估占比 | 平均延迟 | 平均迭代数 | 平均工具调用 |
|------|---------|---------|-----------|------------|
| 闲聊快速路径 | 15% | <1ms | 0 | 0 |
| 语义缓存命中 | 10% | <5ms | 0 | 0 |
| 意图+技能命中 | 20% | ~2s | 1 | 0 |
| 简单问答(无工具) | 25% | ~1s | 1 | 0 |
| 单工具调用 | 20% | ~2s | 1 | 1 |
| 多工具调用 | 8% | ~5s | 2-3 | 2-3 |
| 审批中断 | 2% | 中断 | 1 | 1(需审批) |

### P0 升级效果预估
- **ReAct + CoT**：简单问答准确率 +15-20%（LLM 先分析再回答，减少"直接瞎答"）
- **输出规范**：回答结构化率从 ~40% → ~80%（要点列表、分步骤）
- **工具判断**：误用工具率从 ~12% → ~5%（明确的必须/禁止规则）

### 瓶颈分析
1. **最大瓶颈**：LLM 调用延迟（每次 ~500ms，多轮则累积）
2. **次大瓶颈**：Embedding 计算（意图匹配 + 语义缓存各一次）
3. **可优化**：意图匹配和语义缓存可并行（当前串行）

### 进化建议
1. **预测性缓存**：根据用户行为模式预加载可能的问答
2. **工具结果缓存**：相同工具+相同参数的结果缓存（TTL 5min）
3. **并行意图匹配**：意图匹配 + 语义缓存 + Embedding 计算合并为一次
4. **自适应 ReAct**：简单问题跳过 ReAct 步骤直接回答（减少不必要的 Thought/Action）

---

## 模式二：专家团模式（Expert Team Mode）

### 流程链路
```
用户消息 → pm_analyze(PM分析+分配) → [Send(expert_execute) × N(并行)]
  → debate_round(交叉质询) → pm_evaluate(PM评估)
  ├─ pass → pm_report(最终报告) → END
  └─ fail → increment_round → pm_analyze(返工循环)
```

### 1000次模拟结果分析

| 场景 | 预估占比 | 平均延迟 | 专家数 | 平均轮次 | 总LLM调用 |
|------|---------|---------|--------|---------|----------|
| 2专家+1轮通过 | 30% | ~4s | 2 | 1 | 5(PM+2专家+辩论2+评估) |
| 3专家+1轮通过 | 25% | ~5s | 3 | 1 | 7 |
| 2专家+返工1轮 | 20% | ~8s | 2 | 2 | 10 |
| 3专家+返工1轮 | 15% | ~10s | 3 | 2 | 14 |
| 返工2轮+ | 10% | ~15s | 2-3 | 3 | 15-21 |

### P0+P1 升级效果预估
- **结构化角色 prompt**：专家输出质量 +25%（有思考框架，不空泛）
- **Rubric 评估**：误判率从 ~20% → ~8%（有明确评分维度）
- **辩论轮**：分析深度 +30%（交叉质询发现盲点），但延迟 +30-50%
- **报告结构化**：报告可用性 +40%（有共识/分歧/风险提示）

### 瓶颈分析
1. **最大瓶颈**：串行链路（PM→专家→辩论→评估→报告，每步等上一步完成）
2. **次大瓶颈**：辩论轮增加的 LLM 调用（N个专家各一次辩论）
3. **可优化**：辩论可以只选"分歧最大的2个专家"做交叉质询，而非全员辩论

### 进化建议
1. **选择性辩论**：只对"评估分差 > 3 分"的专家做交叉质询（减少 50% 辩论调用）
2. **专家并行辩论**：辩论轮的 N 个专家调用可并行（当前串行）
3. **增量评估**：PM 评估时只看"辩论修正"部分，而非全量重读
4. **自适应轮次**：简单问题 max_rounds=1，复杂问题 max_rounds=3（动态调整）
5. **专家投票机制**：引入多数投票 + 权重投票，减少对 PM 单点依赖

---

## 模式三：Goal 模式（Goal Mode）

### 流程链路
```
用户目标 → 第0轮: 结构化规划(SubtaskPlan)
  → goal_status_updater(激活首个子任务)
  → llm_call(执行子任务) → evaluator(Rubric评估)
  → goal_status_updater(标记完成/失败)
  → goal_replanner(判断是否继续)
    ├─ 全部完成 → END
    ├─ 有失败 → Reflexion反思 → 动态重规划 → 继续执行
    └─ 未完成 → 继续执行下一个子任务
```

### 1000次模拟结果分析

| 场景 | 预估占比 | 平均延迟 | 子任务数 | 平均迭代 | 总LLM调用 |
|------|---------|---------|---------|---------|----------|
| 3子任务+全部一次通过 | 20% | ~5s | 3 | 3 | 7(规划+3执行+3评估) |
| 5子任务+全部一次通过 | 15% | ~8s | 5 | 5 | 11 |
| 3子任务+1个重试1次 | 25% | ~8s | 3 | 4 | 10 |
| 5子任务+Reflexion重规划 | 20% | ~12s | 5 | 6 | 15 |
| 复杂目标+多次重规划 | 15% | ~18s | 5-8 | 8 | 20+ |
| 失败(超预算/超迭代) | 5% | ~20s | 8 | 10 | 25+ |

### P2 升级效果预估
- **Rubric 评估**：评估准确率 +20%（三维度评分，不再"感觉不行就返工"）
- **Reflexion**：重规划成功率 +35%（有反思的重规划比无脑重试有效得多）
- **结构化规划**：规划质量 +25%（SubtaskPlan 强制输出 id/title/description/dependencies）

### 瓶颈分析
1. **最大瓶颈**：串行子任务执行（当前一次只执行一个子任务）
2. **次大瓶颈**：Reflexion + 重规划的 LLM 调用（反思 + 重规划各一次）
3. **可优化**：无依赖的子任务可并行执行

### 进化建议
1. **并行子任务执行**：`_get_parallel_ready_tasks` 已实现检测逻辑，需接入 `asyncio.gather`
2. **预测性缓存**：相同子任务标题+描述的结果缓存（避免重复执行）
3. **子任务超时熔断**：单个子任务超过 30s 自动标记失败，不阻塞整体
4. **渐进式评估**：子任务执行中做中期检查（不等到完成再评估）
5. **经验库累积**：跨会话的 Reflexion 经验持久化（当前只在单次会话内）

---

## 三模式综合对比

| 维度 | 普通模式 | 专家团模式 | Goal 模式 |
|------|---------|----------|----------|
| 适用场景 | 日常对话、快速问答 | 深度分析、多视角问题 | 复杂目标、多步骤任务 |
| 平均延迟 | 1-3s | 4-15s | 5-20s |
| LLM 调用次数 | 1-3 | 5-21 | 7-25 |
| 质量上限 | 中 | 高 | 最高 |
| 成本效率 | 最优 | 中 | 较高 |
| 可靠性 | 高 | 中（依赖PM判断） | 中（依赖规划质量） |

## 最强进化路线图

### 短期（1周）— 立竿见影
1. ✅ P0: ReAct + CoT prompt（已实现）
2. ✅ P0: 结构化角色 + Rubric 评估（已实现）
3. 并行意图匹配 + 语义缓存（减少普通模式延迟 30%）
4. 选择性辩论（减少专家团模式成本 50%）

### 中期（2-4周）— 质量飞跃
1. ✅ P1: 辩论协议（已实现）
2. ✅ P2: Reflexion（已实现）
3. 并行子任务执行（Goal 模式效率 +200%）
4. 专家投票机制（减少 PM 单点依赖）
5. 自适应 ReAct（简单问题跳过思考步骤）

### 长期（1-2月）— 架构进化
1. 跨会话经验库（Reflexion 经验持久化到 MySQL）
2. 专家团自适应编排（根据问题复杂度动态选择 Debate/Research/Interview 模式）
3. 多模态专家（图片专家、代码专家、数据专家）
4. A/B 测试框架（prompt 版本对比，自动选最优）
5. 用户反馈闭环（评估结果 → prompt 优化 → 效果验证）
